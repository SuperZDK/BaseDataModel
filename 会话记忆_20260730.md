# 会话记忆 — 2026-07-30

> 综合模型 v2.0 → v3.0 迭代 · 输出格式重构 · 积分模型v7.0补充完善

---

## 一、本轮目标

综合模型输出展示方式不符合用户需求（单一方向输出丢失信息），需要重构为同时展示积分和战绩两个维度的信息。

---

## 二、关键问题与决策

### 2.1 综合模型输出格式

**用户诉求**：原始综合模型v1.0只输出单一方向（如"主优势"），积分差距大但战绩反向时信息被吞掉。

**决策路径**：
- 用户否决了"A（隐藏信息自己看）"，选择"B（复合文字展示）"
- 最终输出格式确定为**两行结构**，同尺寸醒目（30px粗体）：
  - 第1行：矩阵方向（附加说明）
  - 第2行：积分情况 | 战绩情况
- 有积分差距（档位2+）：第1行括号内为积分优势方队名，第2行显示该队的积分等级和战绩等级
- 积分均势（档位0-1）：第1行写"积分均势"，第2行写主队战绩情况
- 积分优势方战绩反向时追加 `⚠` 标记

**最终版本**：综合模型v3.0

### 2.2 升班马参照PPG

**用户诉求**：升班马参照应该只在R≤10有用，且不需要按钮。

**决策**：
- 去掉checkbox按钮，R≤10自动使用参照PPG（默认1.05），R>10参照失效，先验PPG=0
- 更新到综合模型v3.0和积分模型v7.0文档中

### 2.3 积分模型阈值

**用户疑问**：是否需要根据近年实际PPG范围修改阈值？

**决策**：不修改。当前阈值合理，与梯队差距自洽。改一个就要重新标定全部，风险大于收益。如有具体案例证明某组梯队差距被持续低估/高估再议。

### 2.4 联赛梯队映射

**用户指出**：积分模型v7.0的使用说明中没有体现联赛梯队对应关系。

**决策**：
- 在v7.0.md的六档判定表中直接加入"梯队差距含义"和"举例"两列
- 删除原先冗余的第四节后半段分表
- 在v7.0使用说明中同步更新
- 确认一个自然偏移现象：档位与梯队差之间存在约半个档位的偏移（如争冠vs欧战PPG差约0.68落入档4区间），这是正常现象，不修正

### 2.5 联赛梯队对应关系（最终确认版）

**每队绝对梯队PPG范围**：

| 梯队 | PPG范围 | 名次 |
|:----|:--------|:----|
| 争冠 | ≥ 2.10 | 1-2 |
| 欧冠 | 1.75 ~ 2.05 | 3-4 |
| 欧战 | 1.50 ~ 1.75 | 5-7 |
| 中游 | 1.20 ~ 1.50 | 8-13 |
| 保级挣扎 | 0.85 ~ 1.20 | 14-17 |
| 降级 | < 0.85 | 18-20 |

**档位与梯队差距对应**（合并后）：
- 档0：同梯队内部 — 中游vs中游（1.30 vs 1.30）
- 档1：相邻梯队交界处 — 争冠vs欧冠边缘（2.21 vs 2.10）
- 档2：跨1个梯队 — 欧冠vs欧战（1.80 vs 1.55）
- 档3：跨2个梯队 — 争冠vs欧战（2.21 vs 1.65）
- 档4：跨3个梯队 — 欧冠vs保级挣扎（1.80 vs 0.95）
- 档5：断档级差 — 争冠vs降级（2.21 vs 0.75）

**重要**：联赛梯队应在积分模型中体现（在详细计算过程中显示），但**不需要在综合模型的输出主结果中显示**。

### 2.6 不允许的行为

用户明确禁止的事项：
- 不许修改现有模型（积分模型v7.0、近期战绩模型v1.0、综合模型v1.0的已有文件）
- 不许在未授权的情况下修改工具输出
- 不许自作主张添加功能（如联赛梯队显示到综合输出中）

---

## 三、文件变动记录

### 新建文件

| 文件 | 说明 |
|:----|:-----|
| 综合模型v2.0_计算工具.html | 首次创建后按用户要求**删除** |
| 综合模型v2.0_输出格式参考.html | 创建后按用户要求**删除** |
| 综合模型v3.0_计算工具.html | 综合模型v3.0的实际工作工具 |
| 综合模型v3.0.md | 综合模型v3.0规范文档 |
| 综合模型v3.0_使用说明.html | 综合模型v3.0的分步操作指南 |
| 积分模型v7.0_使用说明.html | 积分模型v7.0的分步详解指南（含PPG/过渡/档位/梯队） |
| 模型打包_20260730/ | 所有模型文件副本汇集 |
| 会话记忆_20260730.md | 本次会话记录 |

### 修改文件

| 文件 | 改动 |
|:----|:-----|
| 积分模型v7.0.md | 六档判定表新增"梯队差距含义"和"举例"列；删除冗余的分表；新增自然偏移说明 |
| 积分模型v7.0_使用说明.html | 同步更新档位表加举例列；删除冗余的联赛梯队映射对照节；加自然偏移说明 |

### 现有文件（未改动）

| 文件 | 说明 |
|:----|:-----|
| 综合模型v1.0.md | 原始综合模型规范 |
| 综合模型v1.0_计算工具.html | 原始综合模型工具 |
| 综合模型v1.0_网页说明.html | 原始说明 |
| 近期战绩模型v1.0.md | 战绩模型规范（冻结） |
| 近期战绩模型v1.0_计算工具.html | 战绩模型工具 |
| 近期战绩模型v1.0_网页说明.html | 战绩模型说明 |
| 会话记忆_20260728.md | 前期会话记录 |

---

## 四、模型冻结状态

| 模块 | 版本 | 状态 |
|:----|:----|:-----|
| 积分模型 | v7.0 | **冻结** — 阈值不调，梯队映射已合并进档位表 |
| 近期战绩模型 | v1.0 | **冻结** — 无改动 |
| 综合模型 | v1.0 | **冻结** — 保留原始版本不动 |
| 综合模型 | v3.0 | **定案** — 新输出格式两行结构，含使用说明和工具 |
| 盘口模型 | — | 未设计 |
| 特殊情况模型 | — | 未设计 |

---

## 五、下一步方向

1. 用户验收综合模型v3.0工具
2. 开始盘口模型设计（如有Spottery盘口数据）
3. 或特殊情况模型设计
4. 后续可考虑自动化工具（读取本地JSON的File API工具）
